apiVersion: v1
kind: Service
metadata:
  name: rag-service
  namespace: rag-service
spec:
  type: LoadBalancer
  selector:
    app: rag-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
