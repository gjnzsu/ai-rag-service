apiVersion: v1
kind: Service
metadata:
  name: rag-service
  namespace: rag-service
spec:
  type: ClusterIP
  selector:
    app: rag-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
