## 1. Freeze contracts and evaluation inputs

- [x] 1.1 复核 design.md 的 API、预算和统计口径建议，记录首轮固定配置及受支持/不支持的问题范围。
- [x] 1.2 针对固定快照编制四个 Epic 共 12 个问题的输入、预期事实/关系、所需查询范围和基线路由标签；确认不向 Agent 泄漏标签。
- [x] 1.3 为请求、工具结果、findings、分项覆盖、执行摘要和生成状态建立契约测试，包括无 Story 和不完整人口统计。

## 2. Implement deterministic tools

- [x] 2.1 实现固定 scope/快照上下文及 get_epic_backlog，自动翻页、区分 Story/Bug、计算完整统计，覆盖多页与上限截断。
- [x] 2.2 实现有界批量 get_issue_dependencies，复用单事项查询，校验起点/方向/范围，保留单起点故障、来源和关系方向。
- [x] 2.3 建立覆盖账本和证据合并，验证跨 Epic 同项目关系、Done/Done、未知状态、重复证据以及请求中途 active 切换。
- [x] 2.4 实现事实型 finding 的代码谓词核验和来源绑定；验证存在性与否定性结论所需的不同覆盖条件。

## 3. Implement the bounded decision loop

- [x] 3.1 用最小探针验证固定决策模型的结构化动作/工具调用支持，记录实际配置、调用和结果，不变更全局回答模型。
- [x] 3.2 实现独立决策适配器，向模型提供问题、允许工具、证据和剩余预算，接收受支持分析项及 lookup/finish 动作。
- [x] 3.3 实现强制初始读取及最多两次补查的状态循环，独立限制模型轮数、逻辑工具数、底层查询范围和总耗时。
- [x] 3.4 验证无效/越界/重复动作、工具文本提示注入、决策超时和预算耗尽；保留事实并输出正确覆盖和终止原因。

## 4. Integrate platform response and optional API

- [x] 4.1 合并证据后统一编号，适配既有回答/引用边界，支持局部证据报告且不影响旧接口提示和行为。
- [x] 4.2 新增默认关闭入口和配置校验，实现输入错误、主体不可用、部分失败、生成失败的响应契约。
- [x] 4.3 验证结构化 facts/findings 与文字一致、缺失数据不编造、不输出主观评级或延期预测；执行摘要不含隐私推理和凭据。
- [x] 4.4 运行旧接口回归测试及开关关闭测试，确认 /query、/retrieve、Graph API 保持兼容。

## 5. Evaluate and document

- [x] 5.1 建立确定性基线，按预先标注类型做必要查询，复用与 Agent 相同的工具、预算、生成器和证据规则。
- [x] 5.2 运行合成边界/故障场景；每个真实问题每组至少三次，交替运行顺序，记录快照、模型和缓存条件。
- [x] 5.3 逐题核验正确性、引用支持、覆盖诚实、动作必要性，汇总工具/底层查询/模型调用、token 与端到端耗时；记录无增益结果。
- [x] 5.4 更新 Agentic 使用说明及架构图，写明适用范围、已知限制和关闭开关的回滚方式，不覆盖历史 Graph/cache 实验结论。
- [x] 5.5 运行仓库 scripts/quality-check.ps1、实验脚本 lint 和 OpenSpec strict 校验，记录通过/跳过情况。
- [x] 5.6 用户验收：2026-09-21 用户确认本地测试通过；2026-09-21 已归档并同步正式规格，提交与推送尚未执行。
