## 1. 本地基础与合约

- [x] 1.1 核实并固定 Neo4j 镜像和 Python driver 版本，提供本地启动方式、可选依赖和默认关闭配置。
- [x] 1.2 定义 GraphStore、快照 manifest、节点/边/路径/证据及 API 模型，明确作用域、方向、分页和错误合约。
- [x] 1.3 验证图能力关闭时现有服务无需 Neo4j 即可启动且旧接口合约不变。

## 2. Jira 快照与索引

- [x] 2.1 实现只读项目分页采集、富文本转换、重复 token 检测及受控原始快照存储；不输出凭据。
- [x] 2.2 实现 stable ID、parent、issue link 方向规范化、去重、未知类型与跨范围诊断。
- [x] 2.3 实现隔离 Neo4j 图构建和同快照 Chroma/FTS 文本索引，保存版本与校验和。
- [x] 2.4 实现全部就绪后的 active manifest 切换、失败保留旧版本、每请求版本固定及全量重建。
- [x] 2.5 测试多页、失败、重复边、Bug 直接归属 Epic、无归属、范围边界、关系移除和三索引不一致。

## 3. 结构查询 API

- [x] 3.1 实现项目概览与 Epic 下钻，提供全量确定性统计、分页子事项和独立 Epic 状态。
- [x] 3.2 实现方向/类型过滤及最多两跳依赖查询、环保护、100 节点限制和截断提示。
- [x] 3.3 实现 status_category 驱动的 unresolved_pair 筛选，保留 Done/Done 的记录关系。
- [x] 3.4 验证 404/422/503、作用域约束、参数化查询，以及统计不被 top-k/分页影响。

## 4. 图与文本证据

- [x] 4.1 实现显式锚点和同快照 hybrid 种子定位，支持固定 overview/epic_detail/dependencies 意图。
- [x] 4.2 实现 /graph/retrieve，合并关系路径和关联文本并保留边证据、来源、覆盖与降级状态。
- [x] 4.3 实现 /graph/query，复用并适配现有 grounding/引用验证以接收结构事实，支持无解释证据时克制回答和生成失败时返回证据。
- [x] 4.4 测试路径证据不会被 rerank/top-k 丢弃、预算截断、无答案、错误方向、同快照降级及禁止混用默认集合。

## 5. 轻量演示

- [x] 5.1 新增默认关闭的本地静态页面，调用 API 展示 Epic 树/表和详情；无会话和 Jira 写回。
- [x] 5.2 加入局部依赖图、预设关系问题、双方状态、来源证据与快照时间。
- [x] 5.3 检查三个独立操作、空态、错误、截断、内容转义、来源链接及已完成关系的措辞。

## 6. 数据验收与报告

- [x] 6.1 只读生成 AIPLAT 冻结快照，核验实际数量和边；不得将探索时观察的 35 项当作实时硬编码。
- [x] 6.2 标注约 15–20 个真实问题，另建标明合成的两跳、环、跨范围及故障 fixture。
- [x] 6.3 实现 H hybrid+exact、S parent/filter、G 图扩展对照，固定快照、模型和上下文预算。
- [x] 6.4 运行结构精确验收和三步演示，记录证据链覆盖、人工正确率、拒答、延迟和实际成本；说明稀疏图限制及未测指标。
- [x] 6.5 完成仓库适用质量检查，补充本地启动、快照重建、回滚与演示说明；不部署 GKE。

验收证据：docs/evaluation/jira-graph-final-report.md。6.4 已完成可执行评估并记录未测指标：固定生成模型返回 404，真实回答人工正确率/生成延迟/账单金额未测；未以合成结果替代。
